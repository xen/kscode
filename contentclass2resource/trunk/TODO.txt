$Id$

Ошибки:

    1.  Странно, в результате установки ни один объект
        создан не был. DEMO.txt тоже не особо понятен.
        
        !!!! НЕИСПРАВЛЕНО !!!!
        
            В результате установки создаётся экземпляр "Resource container",
            имеющий имя cont_res. В результате установки плагина demo создаётся
            экземпляр "Test content", имеющий имя test_content.

        В таком виде install.txt и demo.txt нам не 
        особенно нужны.       

    2.  Массовые комментарии к INSTALL, DEMO, которые нужно
        прочитать, осознать и исправить.
        
            Прочитано, осознано, исправлено в меру разумения. Ежели неправильно,
            то жду новых замечаний (так оно, скорее всего, и будет).
