$Id$
$URL: https://corp.keysolutions.ru/svn/products/InstallTool/trunk/installtool/TODO.txt $

План работ по <НАЗВАНИЕ>:

сделано    
    1.  (орлов) Сделать фабрику и директиву регистрации фабрики. 
        проверить, что бы вызывалась и ничего не делала, оставить
        там место для кода получения списка скриптов, сортировки
        и запуска;
        
сделано,
исправлено полностью   
    2.  (morthan) Написать код получения списка скриптов, сортировки
        и запуска;
        
сделано
    3.  (орлов) Написать zcml-директиву, регистрирующую записи о скриптах;
    

сделано орловым
    4.  (morthan) Написать класс записи о скрипте;
    
сделано, в результате сварганили свой реестр
    5.  (орлов) Нужно при регистрации скриптов вместо утилит использовать хендлеры.
    
сделано, за счет добавления mix-in AddMixIn, который после
создания вызывает скрипты.
    6.  (орлов) Нужно придумать "отложенный" запуск скриптов, что бы скрипты
        запусакались после добавления объекта, а не после его создания.

Сделано    
    7.  (morthan) Попробуй нарисовать вид перехватывающий эксепшн с ошибкой,
        выведи список скриптов, код завершения и имя сбойного скрипта.
        
Сделано
    8.  (morthan) Аналогично, вид перехватывающий эксепшн с ошибкой сортировки.
    
Тоже сделано, в виде демо
    9.  (орлов) Нужно понять как передать параметры формы в фабрику. Которая (форма)
        кстати, и не вызывается...
           
   10.  (morthan) Написать zcml:factoryform директиву, по заданию. 
        Также прочти README.txt в demo.
        
Конец документа